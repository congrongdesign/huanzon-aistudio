# 环中AIStudio 本地版使用说明（免扣子依赖）

本版本已经改为：
- 本地数据库（JSON）
- 本地图库文件存储
- 本地/自定义大模型与生图模型（OpenAI 兼容接口）
- 默认访客模式（免登录）

## 一、安装

1. 打开安装包：
   - `/Users/congrong/Documents/AI平台/release/环中AIStudio-0.1.0-arm64-本地模型本地库版.zip`
2. 解压后把 `环中AIStudio.app` 拖进 `应用程序`。
3. 首次打开如果提示“无法验证开发者”：
   - 右键 App -> 打开 -> 继续打开。

## 二、首次启动后你会看到什么

- 直接进入应用，不需要登录。
- 会自动使用本地访客用户。
- 项目/聊天/图库数据全部保存在你电脑本地。

## 三、模型配置（最关键）

在应用右上角打开“API 配置”：

### 场景 A：你用本地 Ollama（推荐先测通）

- Base URL 填：`http://127.0.0.1:11434/v1`
- API Key 可以留空（本版本已支持本地地址免 Key）
- 对话模型建议先选：`qwen2.5:7b-instruct` 或你本机已拉取的模型
- 生图模型：如果你本地网关支持 OpenAI 图片接口，再填对应模型；否则生图先用云端接口

### 场景 B：你用云端 OpenAI 兼容平台

- Base URL 填平台地址（例如 `https://xxx/v1` 或 `https://xxx`）
- API Key 填对应密钥
- 对话模型和生图模型按平台可用模型选择

## 四、本地数据存储位置

桌面版本地数据目录：
- `~/Library/Application Support/环中AIStudio/local-data/`

其中：
- 数据库文件：`db.json`
- 本地图片文件：`files/`

## 五、常见问题

### 1）“对话失败：无法连接模型”

请检查：
- 模型服务是否真的在运行（例如 Ollama）
- Base URL 是否正确（本地通常是 `http://127.0.0.1:11434/v1`）
- 模型名是否存在

### 2）“能聊天，不能生图”

说明你当前配置的服务只提供文本模型，没有图片模型接口。
处理：
- 改成支持图片生成的 OpenAI 兼容服务；或
- 单独给生图配置可用网关。

### 3）想切回 Supabase/云数据库

默认是本地模式。如果你后续要强制云模式，需要在运行环境设置：
- `HZ_BACKEND_MODE=supabase`

## 六、你现在可以直接这样测

1. 新建项目
2. 右侧输入“你好，做个自我介绍”测试对话
3. 再输入“生成一张极简风格海报，16:9”测试生图
4. 打开图库确认图片入库

