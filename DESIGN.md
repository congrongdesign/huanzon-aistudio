# DESIGN.md

## 项目与用户画像
环中AIStudio - 面向创意工作者的 AI 设计画布工具，灵感来源于 Lovart.ai 的 ChatCanvas 交互范式。通过 grsai.ai API 接入 ChatGPT Image 2.0 (gpt-image-2) 和 Nano Banana 系列模型作为图像生成引擎。

## Design Tokens

### 色彩
- 背景：zinc-950 深色主题
- 强调色：violet-500/600 用于主要交互按钮和选中状态
- 模型标识色：emerald-500 用于模型标签
- API Key 状态：amber 用于未配置状态，emerald 用于已配置状态
- 画布背景：带点阵网格的深色背景，营造专业设计工具氛围
- 项目侧边栏：zinc-900 背景，与主画布区分
- 登录页背景：#06060c 深黑底 + 鼠标跟随violet/indigo微光(双层延迟) + SVG噪点纹理
- 登录页品牌：HuanZon(6xl渐变白+shimmer流光) + AIStudio(3xl violet→indigo渐变) + SVG Logo(80px+光晕)
- 登录页背景：#06060c 深黑底 + 3层鼠标跟随光(violet/indigo/blue, 500/900/1200px, 0/2.5s/3.5s延迟) + 3层固定环境光(indigo-900左上/violet-900右下/violet-800中心) + SVG噪点纹理
- 登录页按钮：1px轮廓高光边框(flowLight流光扫过边缘,3s循环) + 内部渐变填充 + 内部流光带 + hover外发光(blur-xl) + active白闪反馈
- 登录页文字：HuanZon文字内部流光(textShimmer 3s循环, bg-clip-text渐变白色+亮带在文字内流动)

### 字体
- 字体族：系统默认无衬线字体

## 布局与响应式
- 左侧项目侧边栏（可收起，CSS transition动画）：项目列表，支持创建/删除/置顶/重命名
- 中间为主画布区域（无限画布概念）：图片可任意拖拽、选中、裁剪
- 右侧为AI对话面板（可收起）：与AI交互生图
- 顶部工具栏：模型选择、风格预设、图像比例/尺寸、API 配置、项目/对话面板切换
- API 配置：顶部展开式配置条，支持 localStorage 持久化（Base URL + API Key 两个输入框）
- Base URL 默认节点：https://grsaiapi.com (全球) / https://grsai.dakka.com.cn (国内)
- 未配置时显示琥珀色警告条

## 模型与比例
- gpt-image-2：支持比例（1:1, 16:9, 9:16 等13种）或1K像素值
- gpt-image-2-vip：支持1-4K像素值
- nano-banana 系列（9个变体）：支持比例 + imageSize(1K/2K/4K) 独立参数
- nano-banana-2 系列额外支持 1:4, 4:1, 1:8, 8:1

## 交互模式
- ChatCanvas：右侧对话输入需求 → AI回复含 [GENERATE:prompt] → 自动生图到画布
- 画布拖拽上传：支持拖拽本地图片到画布作为参考图
- 引用图片到对话：选中画布图片后可引用到对话框供AI讨论
- 参考图生图：生成时可附带参考图让模型参考

## PPT AI 工作台
- 导航栏入口：「工作台」（原批量生图），左侧导航栏第二项
- 三阶段工作流：Demo阶段 → 内页制作 → 导出&打包
- Demo阶段5步骤：上传材料 → 生成方案 → 方案审核 → 背景&字体处理 → 元素分层
- 三栏布局：左侧流程导航(56宽) + 中间操作区(弹性) + 右侧审核面板(64宽)
- 底部阶段流程条：5个阶段用不同颜色(violet/sky/cyan/emerald/amber)标识

## 提示词资产管理平台
- 导航栏入口：「提示词」按钮，左侧导航栏第三项
- 对话框快捷入口：发送按钮旁的书籍图标 + 底部工具栏"提示词库"按钮，点击打开内嵌PromptQuickPicker面板(不离开聊天)，可直接搜索/插入提示词；面板内有"管理平台"链接跳转全屏
- 全屏模式：点击后切换navMode='prompt'，复用主内容区域全屏渲染
- 左侧导航栏(224px)：返回画布 + 提示词库切换器(下拉选择/新建库) + 5个导航项(首页/实验室/原子词/组合包/模板) + 底部操作(版本历史/导出/导入/删除库)
- 首页：4张统计概览卡片 + 全网检索(coze-coding-dev-sdk SearchClient + AI摘要 + 一键采纳入库) + 3列使用热度排名
- 实验室：上传2张参考图(authHeaders鉴权) + 多条提示词对比生图 + 星级打分(1-5) + 保存记录 + 高分提示词一键入库 + 测试记录历史
- 原子词/组合包/模板页面：搜索筛选 + 分类面板(右侧可展开) + 内容网格卡片 + 批量选择/插入
- 分类面板：内嵌在内容区右侧(非左侧边栏)，按页面类型自动切换属性分类/场景分类，默认展开，支持拖拽排序，支持子分类和分组
- 多提示词库：支持创建/切换/删除独立的提示词库，导入他人提示词包到空白库不冲突，自动创建默认库
- 版本历史：保存当前库快照 + 恢复历史版本(自动备份当前状态) + 删除旧版本
- 导出/导入：导出JSON提示词包文件(可筛选导出内容) + 导入他人提示词包(可筛选导入+自动重映射ID)
- 创建/编辑弹窗：名称+内容+分类选择+类型专属字段(组合包:原子词下拉选择器按分类分组显示名称, 模板:模型/比例/变量编辑器)
- 组合包原子词选择器：按分类树形下拉选择(一级分类标题+二级分类分组+原子词项)，显示名称而非ID
- 模板变量系统：变量名=一级分类名称，子变量=可选二级分类筛选，默认值=具体原子词/组合包
  - 变量名下拉选择：从原子词/组合包的属性分类中选择一级分类
  - 子变量下拉选择：可选，从该一级分类下的二级分类中选择
  - 默认值选择：从对应分类下选择具体原子词/组合包
  - 提示词内容工具栏：点击"插入变量"在光标位置插入{{变量名}}
- 对话框变量选择器：插入模板后变量可点击展开选择面板
  - 点击变量名 → 展示一级分类下所有原子词/组合包
  - 配置了子变量 → 先展示二级分类分组，点进去选具体提示词
  - 支持原子词和组合包混合变量
- 删除确认：自定义ConfirmDialog弹窗，替代window.confirm
- 一键插入：点击"插入"将提示词追加到对话输入框并自动返回画布视图

## 项目画布侧栏
- 支持折叠/展开：transition动画，折叠时w-0 + 展开条(5px宽按钮)
- 展开条：折叠后左侧出现窄条，点击展开，hover高亮
